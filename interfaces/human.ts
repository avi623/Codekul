import { GpsListener, LightListener } from './gpslistener';
export class Human implements GpsListener,LightListener {
    time = 522;
    walk(): void {
    }

    location() {
    }

    lightUnit() {
        
    }
}